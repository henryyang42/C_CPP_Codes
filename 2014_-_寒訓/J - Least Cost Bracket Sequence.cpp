#include<cstdio>
#include<cstring>
#include<algoirithm>
char s[1000000];
int p[1000000][2], sz;
long long dp[1000000][2][3]; //value, ct,
int main(){
	while(scanf("%s", s) > 0){
		int len = strlen(s);
		memset(p, 0, sizeof(p));
		memset(dp, -1, sizeof(dp));

	}
	return 0;
}
